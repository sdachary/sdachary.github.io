# Portfolio — Environment Variables
# Copy to .env.local for local development
#
# No env vars required for the contact form: it posts to the hardcoded
# `portfolio-contact` Cloudflare Worker, which appends to the Notion
# Contact Submissions DB. The worker's own NOTION_TOKEN is a Wrangler
# secret, set via `wrangler secret put NOTION_TOKEN`.